<mvc:View
	controllerName="cmjs.controller.PageTree"
	displayBlock="true"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:dnd="sap.ui.core.dnd">

	<Page title="Hierarchie">
		<subHeader>
			<OverflowToolbar>
				<ToolbarSpacer/>
				<Button icon="sap-icon://add-document" press=".onNewpagePressed"/>
				<Button icon="sap-icon://delete" enabled="{= ${view>/selectedId} !== null }"/>
			</OverflowToolbar>
		</subHeader>
		<content>
			<Tree
				busy="{view>/busy}"
				mode="SingleSelectMaster"				
				items="{path: 'view>/tree'}" 
				selectionChange=".onSelectionChange">
				<dragDropConfig>
					<dnd:DragDropInfo
						sourceAggregation="items"
						targetAggregation="items"
						dropPosition="OnOrBetween"
						drop=".onDrop"/>
				</dragDropConfig>
				<items>
					<StandardTreeItem 
						title="{view>title}" 
						selected="{= ${view>_id} === ${view>/selectedId} }" />
				</items>
			</Tree>
		</content>
	</Page>
</mvc:View>